<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
    <style>
        .hidden {display: none;}
    </style>
</head>
<body>
    <h1>Quem é esse Pokémon ?!</h1>

    <% if(!pokemon){ %>
        <div>
            <form action="/pokemon/id" method="GET" class="<%=pokemon ? 'hidden': '' %>">
                <input type="text" name="id" placeholder="ID" required>
                <button type="submit">Buscar por ID</button>
            </form>
        </div>
        <br>
        <div>
            <form action="/pokemon/nome" method="GET" class="<%= pokemon ? 'hidden' : '' %>">
                <input type="text" name="nome" placeholder="Nome Pokémon" required>
                <button type="submit">Buscar por Nome</button>
            </form>
        </div>
    <% } %>

    <% if (pokemon) { %>
        <div>
            <p>Nome: <%= pokemon.name %></p>
            <p>ID: <%= pokemon.id %></p>
            <img src="<%= pokemon.sprites.front_default %>" alt="Imagem do Pokémon">
            <p>Habilidades: <%= pokemon.abilities %></p>
            <p>Altura: <%= pokemon.height %> centímetros</p> 
            <p>Peso: <%= pokemon.weight %> gramas</p>
            <p>Tipos: <%= pokemon.types %></p>
            <br>
            <a href="/">Retornar</a>
        </div>
    <% } else if (error) { %>
        <p><%= error %></p>
        <a href="/">Retornar</a>
    <% } %>
        
</body>
</html>