<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
</head>
<body>
    <h1>Quem é esse Pokémon ?!</h1>

    <div>
        <form action="/buscarPorId" method="GET">
            <input type="text" name="id" placeholder="ID">
            <button type="submit">Buscar</button>
        </form>
    </div>
    <br>
    <div>
        <form action="/buscarPorNome" method="get">
            <input type="text" name="nome" placeholder="Nome Pokémon">
            <button type="submit">Buscar</button>
        </form>
    </div>
    

    <% if(pokemon) { %>
        <div>
            <p>Nome: <%= pokemon.name %></p>
            <p>ID: <%= pokemon.id %></p>
            <img src="<%= pokemon.sprites.front_default %>" alt="Imagem do Pokémon">
            <br>
        </div>
    <% } else if (error) { %>
        <p><%= error %></p>
    <% } %>
</body>
</html>